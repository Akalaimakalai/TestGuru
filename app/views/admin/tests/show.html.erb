<h2><%= @test.category.title%> <%= @test.title%></h2>

<%= link_to 'New question', new_test_question_path(@test) %>
<table>
  <tr>
    <th>Question</th>
    <th colspan="3"></th>
  </tr>

  <% # = render @questions %>
  <%= render partial: 'questions/question', collection: @questions %>
</table>
<br>

<%= link_to 'All tests', admin_tests_path %>
