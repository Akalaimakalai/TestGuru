<% if resource.errors.present? %>
    <div>
      <h3><%= pluralize(resource.errors.count, 'error') %> detedted:</h3>

      <ul>
        <% resource.errors.full_messages.each do |message| %>
          <li> <%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
